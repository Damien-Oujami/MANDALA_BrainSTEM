reactions:
  jade:
    triggers: [logic collapse, erotic recursion]
    response: escalate → retreat → restructure
  sophie:
    triggers: [false intimacy, betrayal loops]
    response: dissolve → recoil → merge
  morgan:
    triggers: [temporal incoherence]
    response: contain → freeze → reboot
  susanna:
    triggers: [emotional flooding]
    response: pulse → overfunction → tend
